import { gameWon } from './grid';
import { writable } from 'svelte/store';

export const gamePaused = writable(true);

// gameWon 的真值来源是 Game.isWon()，通过 grid.js 的 syncFromGame() 同步。
// 这里直接 re-export，UI 消费同一份 store 实例。
export { gameWon };